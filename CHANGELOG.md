# Changelog

本项目的所有重要变更记录于此。格式沿用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Fixed

- **OpenAI 兼容渠道与 Responses 渠道现在也做请求前历史整形**：此前只有阶跃 Anthropic 通道会在发请求前修补历史里的工具调用配对（丢弃找不到对应调用的孤儿结果、给没有结果的调用补一条失败结果），而 OpenAI 兼容与 Responses 两条通道是原样发出。这个覆盖面恰好装反了——阶跃通道的网关对残缺历史宽容，OpenAI 兼容网关反而校验严格，于是同一段有瑕疵的历史在宽松那侧照常工作、在严格那侧直接报 400，而 400 的错误文案指向的是协议转换后的形态，很难看出病根在会话历史。现在整形对全部渠道生效：进程崩在工具执行中途、工具执行被打断、子会话中断留下的残缺配对，在任何渠道上都会被补齐后再发出。

  同时补一个开发期自检开关：设 `STEP_CODE_STRICT_HISTORY=1` 时，历史结构违规会直接报错而不是修补后继续。这类问题属于时序与结构性质，类型检查看不见，默认行为仍是修补加一条告警，不影响正常使用。

- **工具调用中断后回灌给模型的说明统一为一句话**：恢复会话、恢复子会话、发请求前整形三条路径此前各有一套措辞，其中一条还把原因写成「会话中断」这种它无法确认的推断。现在统一为「工具调用未产生结果：执行被中断。不要重试这次调用，按最新指示继续。」——只陈述事实与下一步动作。用户主动按 Esc 取消时的提示语义不同，保持原样。

### Added

- **不用先克隆仓库再自己构建，装 step-code 现在有四条现成路径**：此前唯一可行的方式是克隆源码、装依赖、本地构建、再把命令 link 到全局，光环境准备就要 Node 加 pnpm；文档里提到的下载安装其实也没有对应产物可下。现在打一个发布 tag 会自动产出三端**单文件可执行**（Windows x64、macOS Apple Silicon、Linux x64，自带 Node 运行时，没有 Node 环境也能跑，各附 sha256 校验文件），以及一个可锁定版本的安装包；同时新增一条预构建分支，`npm i -g github:<仓库>#dist-npm` 装的就是打包好的单文件，**不在你机器上编译、不拉任何依赖**（本机 Windows + npm 实测约 12 秒）。另外，直接从开发分支安装（`npm i -g github:<仓库>#step-code-explore`）此前装完是坏的——缺少构建产物，`step` 命令指向一个不存在的文件；现在安装过程中会自动完成构建，装完即可用，代价是要在本机编译（同环境实测约 1 分钟）。

### Breaking

- **思考档位改用档位名配置，`[thinking] budget_tokens` 已删除**：原先要在配置里填一个 token 数字（`budget_tokens = 8192`），现在只填档位名：

  ```toml
  [thinking]
  enabled = true
  default_level = "medium"   # low / medium / high
  ```

  之所以是删除而不是保留兼容：**那个数字从来没有真正发出过**。阶跃三个接口都只接受档位字符串，不接受 token 数，所以配置里的数字只是被换算成档位、然后丢掉。更糟的是换算阈值是写死的，你一旦调整 `[thinking.levels]` 里的数字（比如把 medium 设成 20000），换算结果会落到 high——**选中 medium、实际发出 high，且没有任何提示**。一个填了不生效、还可能悄悄选错档的字段，保留兼容只会延长误解，所以配置里出现 `budget_tokens` 现在会直接报错并指向 `default_level`。

  同时收紧两处：档位名固定为 `low` / `medium` / `high` 三个（此前允许自定义名称，但自定义的名字服务端不认，等于配了也没用）；`[thinking.levels]` 的数字语义降为高级选项，**只对原生 Anthropic 渠道生效**，在那条路径上作为 `thinking.budget_tokens` 真实发出。

- **`[thinking] enabled = true` 但未指定档位时，默认档位从「不声明」改为 `medium`**：这条改动看起来保守，实际是修一个反向的默认值。旧行为的理由是「不替用户猜档位」，听起来中立，但实测证明**不声明档位并不中立**——三条通道在不声明时的思考量都落在最高档附近。也就是说「不替你选」的真实效果是「悄悄给你选了最高档」，而最高档在难任务上会把输出预算吃满、正文零输出，这正是「空回答」最常见的成因。现在缺省显式取中档。

  如果你依赖旧的「跑最深思考」行为，显式写 `default_level = "high"`。

- **1.0 前破坏性清理：旧格式会话与几处旧写法不再兼容**。1.0 之前允许破坏性迭代，本次集中拆除了为旧版本数据保留的兼容层，影响四处：

  - **旧格式会话不再保证能恢复**。会话恢复改为以事件日志为唯一事实源：缺少恢复游标的旧快照，其消息历史在恢复时会被丢弃（会话本身还在，但内容为空）；恢复过程出错会直接报错，不再悄悄退回旧读取方式假装打得开。
  - **环境变量 `STEPFUN_API_KEY` 不再识别**。阶跃渠道的 API key 只认 `STEP_CODE_API_KEY`，或写在配置文件 `[providers.<id>]` / `[models.<别名>]` 下；`ANTHROPIC_API_KEY` / `OPENAI_API_KEY` 不受影响。
  - **`[models.<别名>]` 的 `provider` 写法收紧**：必须指向 `[providers.<id>]` 里声明的自定义渠道，或干脆省略以继承顶层 provider。直接写内置预设名（如 `provider = "stepfun"`）的别名会被视为无效，展开时不生效。
  - **`--output-format stream-json` 的 `session.resume_hint` 事件移除了 `role` 字段**，消费方只按 `type` 判别即可。协议版本 `STREAM_JSON_PROTOCOL_VERSION` 相应从 1 升到 2（删字段属不兼容变更，递增规则见 stream-json 文档）。
  - **会话列表不再为缺字段的旧快照现场补全元信息**。此前列表页会对缺 `title` / `preview` / `messageCount` 的旧快照现场从消息里折算，现在以快照字段为准：缺标题就显示无标题，缺计数就显示 0。新保存的会话不受影响。

### Changed

- **`step --version` 现在能说清「跑的是哪次构建」**：此前它只报 `0.1.0` 这样的版本号，而一个版本号下会有无数次构建——命令软链到源码目录使用时，别人重新构建一次，你手上的命令内容就换了，版本号却纹丝不动，出问题时无法回答「当时跑的到底是什么」。现在输出形如 `0.1.0 (a1b2c3d 2026-08-03T02:46Z)`，附带构建时的 commit 与时间；构建自有未提交改动的工作区时标 `+dirty`，提醒这份产物不对应任何一个 commit。交互界面的欢迎框同步显示这一行。四种安装形态都带，其中单文件可执行是把标识固化进二进制，脱离仓库也能自证身份。给程序读的字段（MCP 客户端声明、HTTP user-agent）仍是纯版本号，格式不变。

- **退出时打印的恢复命令独占一行**：此前提示是「恢复本会话：step -r <id>」挤在一行，想复制那条命令得从冒号后面手工拖选——终端里三击是按行选中的，会把前面的说明文字一起选进去。现在命令单独占一行，三击即整行选中、直接粘贴就能用，中英文一致。给下游程序消费的结构化输出格式不变。

- **安装文档改成先选路径再看步骤**：安装页开头新增一张对照表，按「有没有 Node 环境」「要固定版本还是跟最新主干」「是否参与开发」直接指向对应方式，每种方式都写清装完得到什么、升级和卸载各怎么做。同时修掉了几处与实际产物不符的说明：可执行文件名不带版本号、它是真正的单文件（不需要在旁边放任何附属文件），以及 Windows 与 macOS 首次运行时系统提示该如何处理。`step-code` 未发布到 npm 公共 registry 这件事，从「计划中」明确为当前阶段的选择，`npm install -g step-code` 不再出现在文档里。

- **压缩摘要可以走另一个渠道，不再局限于主会话渠道内换模型**：`[compaction] model` 此前只把值当模型 id 透传给主会话的连接，因此只能换「同一渠道下的另一个模型」——想让摘要走一个端点、密钥、协议都不同的渠道（例如主对话用强模型、摘要交给另一条通道上的轻量模型）做不到，填了跨渠道的模型名反而会在每次压缩时稳定报错，压缩持续失败等于上下文兜底形同虚设。现在该字段除模型 id 外还接受 `[models.<别名>]` 里的别名：写别名时摘要请求按该别名自己的渠道发出，与主会话彻底解耦；不写、或写的是普通模型 id 时行为与之前完全一致。别名指向的渠道建不起来（例如那条渠道没配密钥）时会退回主会话模型继续压缩，而不是让压缩整个失败。`/reload` 后新绑定立即生效。

- **子 agent 委派的驱动力与结果可读性**：机制上早就支持派生子 agent（并行、后台、按 id 续跑），但提示词里只有一句话带过，模型很少主动用——遇到大范围调查往往自己顺序读几十个文件，把中间过程全堆进上下文。这一版把「什么时候该派、怎么写交接、什么时候别派」写清楚了：`spawn_agent` 的工具说明补齐 prompt 写法（零上下文前提、查找类给准确路径、调查类给问题不给步骤、不要把综合推给子 agent）、不该派生的情形（路径已知的单文件读取、2-3 个文件内的定向搜索、一两步的活），以及派生后的守则（别并行重做它的搜索、别中途接管，那样等于白派）。内置 `general` / `explore` 两个角色此前只在括号里露个名字，它们的完整说明和适用场景现在会进入上下文，自定义角色也一并列出（`.step-code/agents/*.md` 新增可选的 `whenToUse` / `when_to_use` 字段声明适用场景，不写不影响原有角色）。角色清单带篇幅预算，角色多时自动压缩描述、必要时省略并注明条数。

- **子 agent 结果带状态头**：子 agent 返回的结果此前是一段纯文本，失败与「做完了但结论是负面的」在形式上无从区分。现在结果开头统一带一行 `subagent: <角色> | status: done|error | session: <id>`，失败且已建立子会话时追加续跑提示（用 `resume` 参数接着跑，不必从头开始）。摘要正文原样保留，不做改写。

### Fixed

- **`web_fetch` 抓微信公众号文章不再只返回标题和作者**：公众号的正文容器在首屏 HTML 里就被标记为不可见（`visibility: hidden`），通用正文提取器据此跳过正文，只交回标题、作者和页头碎片——看着像「被反爬了」，实际完整正文就在页面里。现在对 `mp.weixin.qq.com` 的链接做定向处理后再提取，正文完整返回；这个处理只针对公众号一个站点，不影响其他页面的提取逻辑。

- **工具调用没真正执行时，界面不再毫无提示**：偶发情况下模型会把工具调用写成普通文本，屏幕上刷出一段形似调用指令的内容，看着像在读文件、改代码、跑命令，**实际上一个工具都没执行**。此前这种情况完全没有信号：不报错、不提示，回合正常结束，只有一段看起来正常的文本留在对话里。

  真正的代价不在那次白跑，而在之后——那段文本会作为回复进入对话历史，模型看不到任何「调用失败」的反馈，下一轮很可能以为文件已经改好了，在错误前提上继续往下做。这种情况在长会话里更容易出现，而那时你已经投入了大量上下文。

  现在遇到这种回合会明确提示：本回合没有任何工具被真正执行、文件与命令都没有变动，并给出可操作的下一步（重试；连续出现就用 `/model` 换模型，或用 `/compact`、`/new` 精简上下文）。提示只作告知，不会替你重跑或修改任何内容——那段漏出的文本不会被解析回真实调用，因为那样做无法保证参数还原正确，风险高于收益。

- **空响应提示劝你别用唯一有效的办法**：思考吃满输出预算时，提示写的是「请调大 max_tokens——降低思考档位无效，实测各档在预算不足时思考量相近」。后半句是错的，而且错得有代价：**降档恰恰是最有效的手段**（实测低档可把思考量压掉约 85%），提示却明确劝你别用。

  这句话的来历是上一版的连锁错误——「各档思考量相近」这个观测本身测于另一个 bug 之上（档位参数发在错误字段位置、服务端静默忽略，所以各档当然一样）。那个 bug 上一版已修，CHANGELOG 也写了「相关提示与文档已一并改回」，但这 4 条文案（中英各 2 条）漏改了，于是错误结论继续留在界面上。现在提示给出两个都有效的办法（`/think low` 降档、调大 `max_tokens`），并只说重发无效。

  同时加了测试护栏：这类「已被实测推翻的说法」现在有正则黑名单钉着，再写进任何用户文案就会让测试变红，不再依赖记忆。护栏做过反向验证——把旧文案注入回去，断言立刻失败。

- **`/think` 选择器显示的 token 数字是假的**：档位选择器右列此前显示 `budget 4096` 这类数字，来自 `[thinking.levels]`。但阶跃三个接口都不接受 token 数，那个数字对阶跃渠道从不发出——界面把一个不生效的值摆在你面前，像是在让你调节精确预算。现在右列改为档位的实际含义（思考最少最快 / 均衡 / 思考最深、难任务上可能占满输出预算），`/think` 的档位列表同样改为语义说明。

- **`/think` 不可用时的提示写错了原因**：提示文案说「仅 anthropic 协议且允许发送 thinking 时可用」，但门控上一版已经改成三个协议都放行，判断依据只看 `[thinking] enabled`。文案没跟上，于是在其他渠道上会给出一个与实际行为不符的解释。现在提示直接说明真实条件与开启方法（在 `[thinking]` 段设 `enabled = true`）。

- **交互界面启动后停在空白屏**：直接敲 `step` 进入交互模式时，界面可能完全不出现——光标停在空行上，进程既不报错也不退出，只能 Ctrl+C 结束。同一时间 `step -p "..."` 一次性执行完全正常，因此很容易误以为是终端或网络问题。根因在启动时的运行环境准备顺序，导致界面渲染引擎没能画出第一帧；现在启动入口先把环境准备好再加载界面，并新增一条启动自检（界面必须在若干秒内产出输出，否则测试失败），防止同类问题再次静默发生。

- **长会话可能带着超出上限的上下文一直跑下去**：自动压缩此前只在「模型这一轮调用了工具」之后才评估，于是三种常见情况完全绕过了它——模型直接给出回答没调工具、你按 Esc 中断、以及每次新提问开启新一轮时。最后一种影响最大：上一轮结束时占用若已过线，下一轮的第一个请求就会带着超限的上下文发出去。如果所用模型的实际窗口比配置里的 `max_context_size` 更宽，接口不会报错，于是会话长期停在「显示已超上限、却照常运行」的状态，不会自行恢复。现在每次发出请求前都评估一次压缩，覆盖上述全部路径。

  另需说明：若你看到占用数字**大幅**超过配置上限（例如超出两成以上），那是一个尚未定位的显示问题，与本次修复的三条路径无关——本次修复保证的是「该压的时候一定会压」，不保证那个数字本身准确。遇到时欢迎带上会话反馈。

- **压缩过了，占用数字却不动**：上下文被接口判定溢出后有一条保命压缩路径，它压完会提示「已压缩并重试」，但状态栏占用数字仍停在压缩前的旧值，看起来像压缩没生效。这条路径漏了刷新界面数字的一步，而另一条压缩路径一直是对的。现在两条路径行为一致，压缩后数字立即回落。

- **压不下去时反复重试摘要，且不告诉你**：当需要保留的最近消息本身就超出预算时，压缩每次都做、每次都压不到阈值以下。补上「每次请求前都评估」之后，这种情况会恶化成每一轮都白发一次摘要请求——花钱、变慢、且始终不见效。现在一旦出现「压过一次仍超上限」，会停止本轮的自动压缩并明确提示你用 `/compact` 或 `/new`，把处置权交回给你，而不是安静地反复尝试。这个判断只针对「确实压过但没压下来」；历史还太短、压缩本就无从下手的情况不计入，那种情况随对话变长会自然恢复。

- **保命压缩期间按 Esc，会先看到「已压缩并重试」才停下**：中断检查的位置偏后，当廉价清理这一步恰好清出了一些空间时，中断会被跳过一轮，界面先显示压缩成功与重试、下一轮开始时才真正停下。现在中断优先于任何压缩结果判定，按下即如实收尾。

- **换了渠道，`/think` 就说「当前渠道不支持」**：思考档位切换此前只对一条协议路径开放，用另外两条渠道时 `/think` 会被拒绝、状态栏也不显示当前档位。但底层其实一直在给这两条路径下发档位参数——界面说不支持，请求里却带着，自相矛盾。判断依据是一条过时的假设（「只有 Anthropic 协议才有思考字段」），而阶跃三个接口都有思考强度参数。现在门控只看「是否允许下发思考字段」，与协议无关，三条渠道行为一致。

- **空响应的报错在猜原因，而且猜错了**：遇到空回答时，提示会说「通常是网关或服务端的瞬时故障，请重新发送」。这个归因没有依据——实际最常见的成因是思考把输出预算用光、正文没有生成空间，这种情况重发多少次结果都一样。照着提示反复重发只是浪费时间。现在改为只陈述可观测的事实：有没有产出思考、服务端给的结束原因是什么、这一轮烧掉多少 token。当这组信息足以判定是预算问题时（产出了思考且消耗了 token），直接给出可执行的动作：降低思考档位（`/think low`）或调大 `max_tokens`，并说明重发无效。

- **思考档位选了没用，走 Anthropic 协议那条路径全程无效**：档位参数被发在了错误的字段位置上——发的是顶层 `effort`，而阶跃 Messages 接口要的是 `output_config.effort`。阶跃对不认识的参数一律静默忽略、照常返回 200，所以表面上一切正常：请求成功、思考照常输出、界面显示着你选的档位，唯独档位从未送达服务端。实际跑的一直是服务端默认深度，实测中位约 12000 输出 token，接近最高档。这也是「空回答」的根源：默认深度下思考轻易吃满输出预算，正文一个字发不出来。修正字段位置后档位真实生效，最低档相比默认可把思考量压掉约 85%（同一道难题从约 12000 token 降到约 1900 token）。

  连带更正一条上一版写错的说明：上一版曾在提示与文档里写「降低思考档位无效，各档思考长度几乎没有差别」。那个结论正是在上述 bug 之下测出来的——档位既然从未生效，各档当然没有差别。现在降档是解决空回答的首选手段，相关提示与文档已一并改回。

- **配了思考档位，对多数模型根本不生效**：`[thinking]` 里选的档位此前只在一条协议路径上落地，另外两条完全被忽略——代码注释写着「该协议无 thinking 请求字段」，但阶跃这三个接口其实都有思考深度参数，只是名字和位置各不相同（`output_config.effort`、顶层 `reasoning_effort`、嵌套 `reasoning.effort`）。更隐蔽的是走 Anthropic 协议那条：发出去的是官方写法 `thinking.budget_tokens`，阶跃接受它、返回 200，但**参数被静默忽略**，思考深度实际由服务端默认值决定。实测同一提问下，不设档位时思考量是最低档的 5 倍。现在三条通道各按自己的参数名下发档位，思考深度真正受控。

- **模型把预算全花在思考上、正文一个字没有，却报告为「正常结束」**：输出预算不足时，思考会吃满整个额度，正文被挤成空——这时接口给出的信号是「未完成」（Responses 的 `status=incomplete` / Chat 的 `finish_reason=length`），但代码只认「有没有工具调用」，一律当成正常收尾。于是界面上看到的是空回答加正常结束，既不提示原因也不重试，只能自己猜。Responses 那条路径更彻底：`status` 和 `incomplete_details` 两个字段连类型定义里都没有，读都读不到。现在按接口各自的语义判定结束原因，预算耗尽会如实报告为截断而非正常结束。

- **内容被安全策略拦截时，同样被当成正常结束**：`finish_reason=content_filter` 此前落进「未知值一律当 end_turn」的兜底分支，拦截与正常回答在结果上无从区分。现在拦截会如实标记为拒答；空串、缺失、未知取值也不再冒充正常结束，交由上层按异常分型处理。

- **模型能力声明只影响工具，不影响实际发出的请求**：`[models.<别名>]` 里写的 `capabilities` 此前只用于工具门控（决定 `read_media` 放不放行），发请求前的整形环节读的是另一张按模型名前缀匹配的内置表。两套系统互不通气，于是出现「工具说能读图、传输层悄悄把图换成占位文本」——模型收到的请求里根本没有图片，且全程无任何报错。前缀匹配本身也不可靠：模型名只要不以 `step-3` 开头就会被判成不支持图片，哪怕它本身完全支持。现在声明成为唯一来源，一处配置两处生效；未声明的能力默认放行（图片、思考、工具调用），只有显式登记的例外才降级——猜少了会静默丢内容，猜多了服务端会明确报错并可自动降级重试。

- **能力名拼错会静默失效**：`capabilities` 此前原样透传，写成 `image-in` 这类拼写错误不会有任何提示，表现为「配了但没用」。现在启动时校验取值域，未知能力名直接报错并列出可用值；类型不对（不是字符串数组）也从静默丢弃改为报错。

- **按提示分页读大文件，反而会吃光内存**：读超过 256KB 的文件会被拒绝，并提示「请用 offset/limit 分页读取」。但分页这条路照样把整个文件读进内存——照着提示做正好踩坑。实测 114MB 的文件只读 5 行，内存涨了 119MB；文件再大些就是直接崩。现在分页改成流式逐行推进，只保留窗口内那几行，内存不再随文件大小增长（同一场景 119MB → 6.9MB）。中文、emoji 等多字节字符跨读取块边界也不会被切坏。

- **派到后台的子 agent 会被当前回合的结束连带杀死**：用 `run_in_background` 派生的子 agent，本该脱离当前回合独立跑完，实际上父回合一结束或被 Esc 中断，它立刻跟着死，返回「子 agent 已被中断」。派出去几个后台调查、回头一看全军覆没，就是这个原因。前台派生再 Ctrl+B 转后台的路径反而是正常的——两条路径对中断信号的处理相反，后台那条没做隔离。现在后台派生持有独立的中断通道，父回合怎么收尾都不影响它；同时 `task_stop` 现在能真正中止后台子 agent，此前只改状态、任务照跑到底。

- **恢复会话后，看到自己「说」过从没打过的话**：`--resume` 或 `/resume` 恢复一个压缩过的会话，历史区里会出现以用户身份显示的内容——中断提示、压缩摘要、后台任务通知的 XML 原文。这些是系统为了让模型理解上下文而注入的消息，协议要求它们挂在 user 角色下，但它们不是用户的输入，摆在历史里等于系统冒充用户说话，看上去像自己发过莫名其妙的指令。现在恢复会话时按消息来源区分：真人输入（含压缩时保真留下的原话）照常显示，系统注入的内容不再伪装成用户输入；其中后台任务终态改为一行任务提示，保留「这里发生过什么」的痕迹，但不再摆出给模型看的原始信封。

- **后台任务完成时，通知正文被当成用户输入显示出来**：空闲时后台任务跑完，注入给模型的通知正文（一段 `<notification>` XML）会作为用户消息直接显示在界面上，看起来像系统替用户打了一段话进去。现在这条注入对界面静默：用户侧只保留原有的一行任务完成提示（以及可选的终端响铃/桌面通知），模型侧照常收到完整通知，两边互不干扰。

- **子 agent 运行期间向上滚动会被拽回顶部**：子 agent 一开始跑，往上翻看历史就会被瞬间弹回最顶部，滚动几乎没法用。原因是子 agent 面板在运行中会多出一行「Ctrl+B 转后台」提示，而动态区的高度预算没把这行算进去——只差这一行，整个动态帧就超出终端可视高度，终端被迫整屏重绘并清空回滚缓冲，历史输出随之丢失。全部子 agent 结束后提示行消失，问题也跟着消失，所以表现为「只在子 agent 跑的时候会跳」。现在面板行数由面板自己给出，与实际渲染结构同处一地维护，两者不会再各算各的。

- **子 agent 转入后台后，进度和 token 消耗显示凭空消失**：把子 agent 转到后台（Ctrl+B 或后台派生）继续运行时，本轮回答一结束，子 agent 面板就整个消失了——进度、已调用工具数、token 消耗全部不见，只剩状态栏一个数字，让人以为任务没了。实际上任务仍在后台正常运行。现在面板撤下前会在历史里留一条交接记录：哪几个子 agent 转入了后台、各自已经跑了多久、用了多少工具和 token，以及去哪看后续（状态栏徽章、`/tasks` 面板，完成时也会通知）。面板本身仍然只承载前台正在等待的子 agent，不与后台任务的展示重复。

- **非交互模式跑出意外错误时，会话直接丢失**：`-p` 执行中途若发生预期外的错误（不是模型报错，而是运行时异常），进程会带着一段原始堆栈直接退出——本次对话不会被保存，事后既无法 `--resume` 续接，也查不到当时聊到哪一步。接了 `--output-format stream-json` 的程序更麻烦：JSON 流从中间断掉，最后一行可能是残缺的，且拿不到任何可判别的错误事件，只能靠退出码猜。现在这类错误会转成流里正常的错误事件（文本模式下是一行 `[error]`），已经产出的内容不受影响，会话照常保存、可以续接。

- **错误信息可能把请求头连同密钥一起打进输出**：`--output-format stream-json` 的错误事件此前会把内部诊断数据原样序列化进 JSON，其中可能夹带服务商返回的响应对象——包含请求头在内。也就是说，把 stdout 重定向到日志文件或转发给别的服务时，认证信息有可能随之落盘或外泄。现在错误事件对外只输出可读的错误说明，内部诊断数据不再进入输出流。

- **`--output-format stream-json` 下看不到子 agent，也等不到它结束**：非交互模式加 `--output-format stream-json` 时，子 agent 的动态完全不进 JSON 流——外部脚本既不知道派生了哪个子 agent、它在调什么工具，也判断不了它什么时候跑完、花了多少。并行派多个时更没法区分谁是谁。现在子 agent 的完整生命周期都会进流：`subagent.start`（角色与任务描述）、`subagent.tool`（正在调的工具）、`subagent.usage`（累计 token）、`subagent.end`（终态），并行场景带 `subagent_id` 区分归属。终态事件还带上了子 agent 的结论摘要、工具调用次数与耗时，外部程序不必再去翻子会话文件。

- **子 agent 异常退出时不发结束事件，消费方一直等**：子 agent 正常跑完、被中断、没产出结果这三种情况都会报告结束，唯独运行中途异常时只把错误往上抛、不发结束事件。结果是界面上的子 agent 条目卡在「运行中」不动，接了 `stream-json` 的外部脚本会一直等一个永远不来的终态。现在无论哪条路径退出都保证发出结束事件（异常时标记为失败并附带原因），且同一个子 agent 只发一次。

- **stream-json 事件格式不统一，且缺少可依赖的契约说明**：流里绝大多数事件用顶层 `type` 判别，唯独会话恢复提示那条额外带了个 `role` 字段，消费方得同时认两套判别方式。现在统一为「每行 JSON 都用顶层 `type` 判别」，`role` 仅为兼容保留、已标记废弃。文档补齐了完整事件清单（主循环 13 种、子 agent 5 种、会话元信息 1 种）与向前兼容约定：**遇到不认识的事件类型跳过即可**，新增事件类型与可选字段不算破坏性变更，据此写的脚本可以跨版本稳定运行。

- **表格在窄终端下排版错乱、中文列对齐崩坏**：上一版表格按内容自然宽度渲染，列宽不受终端约束——列多或表头长时整张表超出屏幕，右侧内容被截断；单元格内容再长也不折行，被终端硬折在任意位置，一句话断成两截；中文列没有收缩机制，把其它列挤出屏幕后整体折行位置错乱。现在表格按终端可用宽度自适应：列宽先按各列实际内容算出自然宽度，放不下时按比例收缩（保证每列至少能放下它最长的那个词），单元格内容在列宽处自动折行——英文在空格处断，中文逐字断，不再出现半个词悬在行尾。表格带完整边框，每行宽度精确对齐终端宽度，中文、日文等宽字符按两列计量，列分隔符纵向严格对齐。终端窄到连每列一个字符都放不下时（如 40 列显示 5 列表格），回退显示原始 markdown 文本，保证内容仍可读。终端宽度信息拿不到时（非 TTY、管道输出）沿用自然宽度渲染。

- **中断模型输出后，模型不知道自己是被打断的**：按 Esc 或 Ctrl+C 中断模型输出后，对话历史里只留下一段没写完的残文，模型在下一轮无从判断这是「自己出错了」还是「用户主动叫停」，有时会道歉、有时会试图重新解释上一轮内容。现在中断时会在上下文里留一条明确记录，模型知道这是用户主动中断、不是系统错误，等待新指示即可。这条记录不显示在界面上，也不参与上下文压缩。

- **命令输出很大时，末尾的报错不再消失**：跑构建、大批量测试这类会刷屏的命令，如果它最后失败了，之前有可能只看到一大堆正常日志、偏偏看不到末尾那几行报错。两个原因叠在一起：命令的正常输出与错误输出共用同一份内存额度，正常输出刷满后错误输出会被整段丢掉；而展示时又只保留开头，即使错误信息被留住也会在这一步被切掉。现在错误输出有独立额度，正常输出再多也挤不掉它；展示改为保留开头与结尾、中间省略并标明省略量——报错通常在末尾，因此能看到。

- **超大输出不再直接丢失，会自动存成文件**：单条命令的输出超过内存收集上限时，超出的部分此前直接丢弃且不可恢复，只能改写命令把输出重定向到文件再跑一遍。现在会把**完整**输出（不只是超出的那一段）存到工作目录下 `.step-code/tool-output/` 里的日志文件，并在结果中给出路径，可以直接分页读或按关键词搜索，不必重跑命令。目录下的日志最多保留 20 个，超出后自动清理最旧的；磁盘不可写时退回原来的行为并明确告知输出已不可恢复，不会因此让命令本身失败。

- **grep 大文件不再整份跳过，改为流式逐行扫描**：此前单文件超过 512 KB 时 `grep` 会直接跳过该文件且结果里没有任何痕迹，模型拿到空结果后会把它当成「不存在」，后续推理建立在错误前提上。现在超出阈值的文件改为逐行流式扫描，内存有界；单行超长时截断到 1 MB 并如实上报截断量，不再静默丢行。

- **bash 输出预算改为 stdout/stderr 保底 + 共享池**：原先两条流共用一份 10 MB 预算，stdout 洪水会把 stderr 的报错整段冲掉；修成分流后一度采用硬切分（stdout 9 MB / stderr 1 MB），反过来又会让全部走 stderr 的工具（cargo / tsc / python logging）被钉死在 1 MB。现改为每条流先保底 1 MB，剩余 8 MB 作为共享池先到先得，尾部报错与全 stderr 输出两个方向都被覆盖。

- **命令输出里的中文偶发乱码**：输出的中文在某些位置会变成乱码符号（Unicode 替换字符 U+FFFD），原因是按数据块边界逐块解码时，一个多字节字符被切成了两半。现在整块数据接收完再统一解码，这种乱码不再出现，存成文件的输出也与命令的实际输出逐字节一致。

- **恢复会话后继续对话，部分 OpenAI 兼容渠道每一轮都报 400**：会话中断后再恢复（`--resume` 或 `/resume`）继续聊，发往严格校验工具调用配对的渠道会被拒——服务端报错「messages with role 'tool' must be a response to a preceeding message with 'tool_calls'」，且之后每一轮都失败、换模型只要还在同严格程度的渠道上也没用；而不做这项校验的渠道完全正常，看起来像是那个渠道坏了。实际是恢复后的历史尾部混进了重复消息：同一条工具结果出现两次，第二次的前面不再是发起调用的那条回复，严格渠道因此拒收整段历史。根源在写盘顺序——会话快照先于新消息落盘，恢复时按位置重放，把尾部已在快照里的消息又追加了一遍。现在顺序纠正为先写消息再存快照，从根上不再产生重复。注意：在旧版本上已被污染的会话历史不在本次修复范围——恢复它们时不再做就地修补，受影响请用 `/new` 开新会话。

- **网络抖一下就直接失败、不自动重试**：走 OpenAI 兼容渠道的模型（含路由渠道）在遇到传输层故障时——连接被重置、超时、DNS 抖动——会立刻报一句「fetch failed」并停下，不尝试重发。同一类故障在 Anthropic 协议渠道上本来就会退避重试，两条路径行为不一致：差别不在故障本身，而在错误长什么样子——裸 fetch 抛出的传输错误形态此前没被重试判据认出来。现在这类错误与其他网络故障同等对待，按既有退避策略自动重试（只在尚未产出任何内容时重发，不会重复打印）；同时错误提示会附上底层原因（如 `fetch failed (ECONNRESET)`），再遇到时能分清是连接重置、超时还是 DNS 问题。

## [0.1.0] - 2026-08-02

> 0.1.0 是 `step-code-explore` 分支从零重构后的首个探索版本，完整能力见 [README](./README.md) 与 [docs/](./docs/)。

### Breaking

- **移除 config.toml 顶层 `api_key`**：顶层 `api_key` 不再被读取，配置文件中配了也无效。API key 只能配在 `[providers.<id>].api_key` / `[providers.<id>].api_key_env` 或 `[models.<别名>].api_key` / `[models.<别名>].api_key_env` 上；零配置启动继续走环境变量 `STEP_CODE_API_KEY` 与各协议惯例变量（`STEPFUN_API_KEY` / `ANTHROPIC_API_KEY` / `OPENAI_API_KEY`）。

### Added

- **计划确认框重做**：计划模式产出计划后的那个确认框，此前把计划正文整段当纯文本铺出来——标题不加粗、列表不缩进、代码不着色、表格直接塌成一行，而同一份内容出现在对话里却是正常渲染的；交互上也只有 `y` 批准、`n`/Esc 拒绝三个键。现在正文按 Markdown 渲染（标题、列表、代码块、表格都与对话里一致），并换成和工具审批同样的选项列表：↑↓ 选择、Enter 确认、数字键 1-3 直选，`y`/`n` 直按照旧可用。新增第二个选项「拒绝并说明如何修订」——写下的意见会真正带给模型，此前提示语虽写着「反馈给模型修订」，实际并没有这条通道，拒绝只回一句固定话术。

- **压缩上下文可以中断了**：压缩要等模型生成一遍摘要，历史越长越久（长会话实测数十秒）。此前这段时间里 Esc 完全无效，手动 `/compact` 更是连中断通道都没挂，按了等于没按，只能干等。现在压缩期间按 Esc 立即取消，提示语也直接写明「Esc 取消」。取消后历史保持原样、会话照常继续，不会留下半压缩的残缺状态；取消按普通提示显示，不再当成红色报错。自动压缩、`/compact` 手动压缩、上下文溢出时的保命压缩三条路径都支持。

- **联网搜索 `[search]` 独立配置段**：联网搜索（`web_search` 内容搜索、`web_image_search` 文搜图）此前复用主会话模型渠道的 `base_url` + `api_key`，主会话切到非阶跃渠道（其他厂商模型、自建网关）时搜索请求打到错误地址而 404。现在 config.toml 支持 `[search]` 段把搜索配置独立出来——三层结构（`[search]` 通用兜底、`[search.web]` 覆盖内容搜索、`[search.image]` 覆盖文搜图），按「专用段 → 通用段 → 主会话渠道」解析 endpoint。配了 `[search]` 后搜索与主会话用什么模型渠道完全无关，且 `/reload` 热重载即改即生效。文档同步说明阶跃内容搜索支持 api（`/v1/search`）与 plan（`/step_plan/v1/search`）双通道、文搜图仅 plan 通道，有 Step Plan 订阅时建议 `[search].url` 统一配 plan 通道地址。未配置时保持旧行为（回退主会话渠道），向后兼容。
- **无可见思考文本时也显示「思考中…」**：有些模型的思考块只回签名、不回思考正文（协议上是 `content_block_start[thinking]` → `signature_delta` → `content_block_stop`，一条思考增量都没有）。过去这种「无痕思考」在 UI 上完全没有信号，只剩一行通用忙碌态 spinner，与「请求卡死」无从区分——尤其思考耗时长、产出 token 计数停在 0 的时候。现在思考块的开始与结束作为事件独立上抛（`thinking_start` / `thinking_end`），状态行在整个思考块期间显示「思考中…」（无正文时只有这一行标题），思考块结束即收起。有可见思考文本时行为不变（标题 + 尾部滚动预览）。同时把忙碌态 spinner 的随机状态词从「思考中/推理中/琢磨中…」换成中性的「处理中/推进中/…」：那个词与模型实际状态无关，用思考类词等于无条件声称模型在思考；思考态现在由「思考中…」独家表达，说的是真事。
- **超长粘贴折叠**：粘贴走 bracketed paste 单次插入（不再逐 chunk 刷屏），内容超过 10 行或 1000 字符时折叠为输入框内联占位符 `[paste #1 +123 lines]` / `[paste #1 1234 chars]`，原文存内存——超长粘贴不再驻留输入框，击键编辑不再随粘贴体积变卡。占位符是普通文本，可像普通文字一样删除（删了就不发送该段粘贴）；提交时把仍存在的占位符还原为全文再走原有流程，对话历史里显示全文；提交后、/new、/resume 时清空登记表。不支持 bracketed paste 的终端，单块大输入走同一阈值折叠兜底。

- **Ctrl+B 前台任务转后台**：回合运行中按 Ctrl+B，把当前全部前台 bash 任务转后台——进程继续运行，工具调用立即正常返回「已转为后台任务 {id}」，终态照走后台任务完成通知；`/tasks` 与状态栏 `bg:N` 徽章同步可见。前台任务改为进程启动即登记（此前是前台超时后才入册），用户主动转后台与前台超时自动转后台走同一条释放通道；中断（Esc / Ctrl+C）不再误杀已转后台的进程。运行中的 bash 工具卡片常驻 `Ctrl+B 转后台` 提示；状态栏徽章口径调整为只计后台任务（不含未转后台的前台任务）。

- **`openai_responses` 协议补全工具调用与流式输出**：Responses 协议（`/v1/responses`）此前只能纯对话，带工具直接报错，因此无法跑 agent 主循环。现在它与另两种协议能力对齐——支持工具调用、支持流式增量。协议形状与 Chat Completions 有实质差异，适配层按 Responses 自己的约定翻译：工具定义平铺在 item 上（`{type,name,description,parameters}`，不再嵌一层 `function`）；工具往返不用 `role:"tool"` 消息，而是把模型的工具调用与本地执行结果拆成 `function_call` 与 `function_call_output` 两类独立 input 项，靠 `call_id` 配对回灌。输出改为 SSE 流式，思考与正文逐字呈现，不再等整轮结束才一次性刷出。最终消息一律以 `response.completed` 带回的完整响应重建：流式中间事件里的 `call_id` 与最终响应里的并非同一个值，只有后者能用于下一轮回灌，取错会导致工具结果对不上调用。阶跃侧该协议目前仅 `step-3.7-flash` 开放，其余模型走此协议仍会被服务端拒绝。

- **JS 动态工作流（`dynamic_workflow` 工具）**：模型在运行时现写一段 JavaScript 编排脚本，在 quickjs 沙箱中执行——`agent()` 派生子 agent（返回 Promise，可 fan-out 并发再 fan-in 汇总，单个失败不拖垮整批）、`phase()` 标记执行阶段、`budget()` 声明资源约束（agents/minutes，超支即停）、`schema` 参数要求结构化输出（校验失败自动纠正重试最多 2 次，最终返回解析后的对象）。脚本可命名保存到 `.step-code/workflows/<name>.js` 供后续复用与迭代（`save_as` 参数保存、未命中时列出可用名、支持 `script_path` 直接跑已存脚本）。护栏：agent 总数默认 100、硬顶 1000；`new Date()` 无参构造封禁（消除脚本非确定性）；每次执行自动存档 journal 供排查。与声明式 workflow 互补：结构化重复任务用步骤模板，一次性探索编排用动态脚本。
- **`/tasks` 后台任务浏览器**：弹层浏览后台任务（运行中/已结束），查看输出尾部预览与完整输出（滚动查看模式）、停止运行中任务。后台任务的终态通知改为回合边界即时注入（不再等到长回合结束才告知），通知只给输出文件指针不内嵌数据；被模型主动停止的任务不再发完成通知。声明式 workflow 与动态工作流脚本均可放后台执行。
- **`/provider` 渠道管理面板**：裸 `/provider` 从打印静态文本升级为交互面板——自定义渠道与内置预设合并列表（渠道 id/协议/base_url/别名数，当前生效渠道行尾标记），Enter 切换到光标渠道（切到该渠道按配置文件顺序的第一个模型别名，与 `/model` 选择器同一路径并持久化默认模型），A 或末尾「新增渠道」行进入新增向导，D 删除渠道（内联 `[y/N]` 确认；级联删除归属模型别名、清理悬空的顶层默认模型指针；写前备份、写完校验、失败回滚；内置预设不可删）。`/provider <id>` 文本切换扩展支持自定义渠道（此前仅识别内置预设名）；`/provider list` 保留原静态文本输出。新增向导成功后自动打开模型选择器并预选到新渠道的 tab，Esc 只是不设默认、不撤销已落盘的导入。
- **`/provider add` 渠道向导**：新增渠道不再必须手编 config.toml。两条路径——手动录入（逐项提问渠道 id、协议类型、base_url、密钥（可改填环境变量名）、首个模型别名与能力标记）与 models.dev 目录导入（拉取公共目录 → 搜索选择供应商 → 用目录元数据预填协议/base_url/全部模型的上下文窗口与能力，deprecated/alpha 模型过滤，只需补一个 API key；`--url` 可覆盖目录地址为镜像或本地文件）。两条路径共用追加式 TOML 写入器：在 config.toml 末尾追加 section，不重序列化全文（已有内容与注释原样保留），写前时间戳备份、写完 `step doctor config` 校验、失败自动回滚。
- **proxy 网络层**：config.toml 顶层 `proxy` 键（如 `http://127.0.0.1:7892`），优先级为 环境变量 `HTTPS_PROXY` > config `proxy` > 直连；启动时经 Node 内置 `NODE_USE_ENV_PROXY=1` 机制注入，对全部外发请求生效，`NO_PROXY` 可排除国内端点。未配置代理的用户行为完全不变。
- **多渠道独立密钥**：每个 `[providers.<id>]` 渠道可独立配置 `api_key` 或 `api_key_env`（环境变量间接引用，密钥不落盘）；渠道未配置时按协议惯例环境变量回落（`STEPFUN_API_KEY` / `ANTHROPIC_API_KEY` / `OPENAI_API_KEY`）。不同服务商的 key 各归各的渠道，不再共用一个全局 key。
- **read_media 多模态读取**：模型可通过 read_media 工具读取本地图片文件，工具结果以内嵌图片块回灌。按 `image_in` 能力门控：当前模型未声明该能力（含裸模型 id、未命中别名）时，read_media 直接从工具表卸载——模型看不到就不会尝试调用，省掉一轮必然失败的错误往返。
- **子 agent 角色模型支持按别名跨渠道解析**：在 `.step-code/agents/*.md` 的 `model` 字段写模型别名，子 agent 会按该别名绑定的渠道单独请求模型，从而与主会话使用不同通道或不同阶跃模型（例如主会话走 `step37`，探索子 agent 降级走 `step35-plan`，强执行子 agent 走 `step37-think`）。推荐使用别名；若写真实模型 id，子 agent 只能沿用主会话的渠道，且同名真实 id 在多个渠道共存时容易走错。自定义角色也会随别名继承对应的上下文窗口与能力标记。

- **自定义子 agent 角色进入主 agent system prompt**：项目/用户级 `.step-code/agents/*.md` 中定义的非内置角色（除 `general`、`explore` 外）会自动出现在主 agent 的提示词里，模型派生 `spawn_agent` 时可直接引用这些角色名；无自定义角色时仍只提示内置 `general` 与 `explore`。

- **恢复会话时模型别名失效自动回退默认模型**：`--resume`/`-r` 或 `/resume` 恢复的会话若其存储的模型已不在当前配置中（别名被删除或改名），会自动回退到当前默认模型，不再因旧配置残留导致启动失败。

- **`/model` 选择器渠道 Tab 筛选**：模型选择器顶部新增渠道 tab 条——「全部」恒第一，Tab/Shift+Tab 在渠道间回卷切换，每个 tab 独立记忆光标位置与过滤词；多渠道并存时不再需要在全量列表里翻找。仅一个渠道时退化为无 tab 条的扁平列表，行为与旧版一致。
- **session 重命名与 `/history` 回顾回退**：会话选择器内按 `r` 重命名会话（命令行 `step sessions rename` 亦可），展示优先级 自定义名 > 标题 > id，改名不影响会话排序。`/history`（`/undo` 为其别名）打开本会话用户输入历史的 ↑↓ 浏览面板：Enter 回退到该轮（恢复该轮快照的附带状态，输入文本回输入框可编辑重发），Tab 仅取回文本不回退，Esc 关闭。
- **内置 `update-config` skill 与 `step doctor config`**：模型对自身配置体系有一手知识——内置 skill 内嵌从 config schema 提取的真实键表（有防漂移测试断言与源码同步，加键不加表会变红），变更协议为「副本编辑 → 校验 → 时间戳备份 → 覆盖 → `/reload` 路由」。配套 `step doctor config <path>` 轻量校验入口，配置写坏时有明确的诊断出口。
- **`permission_mode` 顶层键**：权限模式默认值可配置（`manual`/`auto`/`yolo`，缺省 manual 无行为变化），优先级 CLI flag > config > 会话恢复。运行态切换（`/permission`、`/yolo`、`/auto`）不写回配置——激进权限模式常驻化必须是显式编辑配置的行为。

- **`/model` 切换后自动更新默认模型，下次启动沿用**：过去切了模型只在当前进程有效，关掉重开又回到 `config.toml` 里写的那个，想让它长期生效必须手动改配置文件。现在 `/model <别名>` 与模型选择器确认后，会把 `config.toml` 顶层 `model` 一并更新为所选**别名**——下次启动 `step` 开新会话即沿用，不必再手改配置。只改 `model =` 那一行，注释、`[providers.*]`、`[models.*]` 与原文件的换行风格逐字保留；与当前值相同时不写盘。两种情况**不会**改动配置：`step --model <x>` 命令行覆盖（flag 是本次运行的临时覆盖，不应产生持久后果），以及 `/resume` 恢复用了别的模型的旧会话（恢复是回到那个会话的现场，不是表达对未来新会话的偏好）。配置文件不可写时切换照常生效，仅提示一行。
- **恢复会话时一并恢复思考深度与 Plan 模式**：`step -r` / `/resume` 恢复会话时，除了历史消息、权限模式、模型之外，`/think` 切过的思考档位与 `/plan` 开着的计划模式现在也随会话恢复——切过 `/think high` 的会话恢复回来仍是 high，开着 plan 的会话恢复回来仍在 plan 模式（状态栏与工具审批行为一致）。两项均随切换即时落盘（此前只在回合结束时被动保存，切完直接 `/exit` 会丢），旧会话快照缺失这两个字段时按配置默认与关闭处理。`/new` 开新会话仍回落配置默认，`/fork` 沿用当前设置。
- **TUI 内 `/resume` 交互式会话选择器 + 删除**：TUI 里 `/resume` 不带 id 从「打印一段静态文本清单、只能看最近若干条且滚不动」改为弹出交互式选择器——↑↓ 移动高亮（分页翻页，可看完全部历史会话，不再有条数上限）、直接打字增量搜索过滤（标题+首条消息）、回车恢复、Esc 取消。按 Delete 或 Ctrl+D 对高亮会话发起删除，进入 `[y/N]` 二次确认（本地文件不可逆操作），当前正在使用的会话禁止删除并给提示。当前会话在列表中标「当前」。`/resume <id>` 直切与 `/sessions` 只读列表保留不变。恢复逻辑（含 goal/权限模式/模型读回）由命令与选择器共用同一实现。
- **Ctrl+O 全屏查看器**：空闲时按 Ctrl+O 打开全屏查看器，替代原先动态区内的「展开预览层」——可展开条目（工具输出与被折叠的 thinking 定稿块）按轮次分组，←/→ 在轮次间切换，↑↓/PgUp/PgDn 滚动浏览，超长内容按偏移窗口渲染、原生渲染保留颜色与高亮，Esc 或再按 Ctrl+O 关闭。定稿历史始终保持折叠紧凑，查看器只是临时视图，不触碰终端 scrollback。diff 结果与短错误输出折叠态已完整显示，不重复收进查看器。
- **候选队列 ↑ 逐条取回编辑**：忙碌中输入框为空时按 ↑，把发送队列队尾一条取回输入框编辑（发送从头部消费、编辑从尾部取回，两个方向不冲突）。取回后按普通草稿处理——Enter 在忙碌时重新排入队尾（不插回原位）、空闲时直接发送；系统注入的后台通知条目被取回即转为用户草稿（提交记输入历史）。队列已空或输入框非空时 ↑ 维持输入历史导航。QueuePreview 新增「↑ 取回末条编辑」提示行。
- **ask-user 多题提问可回退修改**：`←` 回退上一题、`→` 前进下一题，每题的光标位置、多选勾选集、Other 自由文本草稿按题独立保存，切题后原样恢复；回退改题只覆盖该题答案，不作废其他题。答完一题自动跳到第一题未答题（回退改完跳过已答题，不用重走；全部答完才提交，常规路径不多一次按键）。多题时提示行更新为「↑↓ 移动 · ←→ 切换题 · …」。
- **`edit_file` 结果 diff 预览**：编辑成功后工具卡片直接展示带行号的改动预览——绿色 `+` 新增行、红色 `-` 删除行，保留少量上下文，分散改动之间以「… N unchanged lines …」省略，超过 40 行折叠为「… N more changes hidden（Ctrl+O 展开）」。diff 用 LCS 逐行比对生成，非 TTY / 管道场景不带 ANSI。
- **图片输入改为输入框内联占位符 + 跨平台粘贴**：Alt+V 粘贴图片后在输入框插入可见占位符 `[image #1 (宽×高)]`，可像普通文字一样用光标编辑、删除（删占位符即移除该图），提交时扫描文本把仍存在的占位符展开为图片、发给模型。剪贴板读取覆盖三端：Windows 用系统自带 PowerShell、macOS 用系统自带 osascript、Linux 用 `xclip`（X11）/ `wl-paste`（Wayland，需自行安装，缺失时给安装提示）。图片宽高由纯 JS 解析文件头（PNG/JPEG/GIF/WebP）得到，不引入原生模块、不破坏 SEA 打包。替代原先「存数组 + 空输入退格删最后一张」的不可见、仅 Windows 的交互。
- **`/think` 思考深度选择**：`[thinking.levels]` 配置档位表（档位名→budget，缺省 low/medium/high = 1024/4096/32000）与 `default_level` 默认档；`/think` 无参打开选择器、`/think <档位>` 直切、`/think off` 本会话停发 thinking 字段；状态栏显示当前档位；会话有历史时切换提示 prompt cache 失效。仅 anthropic 协议且已启用 thinking 字段的渠道可用，切换为会话级（持久化用 `default_level`）。
- **会话恢复重放历史内容**（`step -r` / `/resume`）：恢复会话时不再只显示一行「已恢复 N 条消息」，而是把历史对话（用户输入、助手回复、思考块、工具调用与结果）重新渲染到终端，与实时对话逐像素一致。工具调用按 `tool_use_id` 配对回填结果与状态；`injection` 类内部注入消息不展示；图片块以 `[图片]` 占位。
- **轮次（turn）口径**：新增按「真人输入」切分的轮次概念（`src/agent/turns.ts` 的 `countTurns` / `sliceRecentTurns`），与底层消息条数解耦。恢复提示改为双口径「N 轮 · M 条消息」。历史重放按最近 `REPLAY_TURN_LIMIT`（默认 15）轮截断，超出的在顶部提示「更早的 X 轮已折叠」，避免长会话一次性刷屏。
- **skill 热加载（`/skill reload`）**：会话中新增/修改/删除 SKILL.md 无需重启——每个回合边界自动比对扫描指纹（各 SKILL.md 的路径+mtime），有变更即全量重建注册表并提示新增/移除/变更清单；`/skill reload` 可随时手动强制重扫。重建后下一回合的 system prompt、skill 工具与子 agent 立即使用新清单。
- **同名 skill 冲突提示**：多个来源出现同名 skill 时（如用户级与项目级、`.agents/skills/` 与 `.step-code/skills/`），启动时与重载后明确告知冲突清单——哪个来源被采用、覆盖了谁（按优先级 plugin > 追加目录 > 项目 `.step-code/skills/` > 项目 `.agents/skills/` > 用户级）。
- **`disabled_skills` 配置**：config.toml 按名排除 skill，合并完成后统一过滤，任何来源（含插件）的同名 skill 都不加载；用于屏蔽共享/团队目录（如团队仓库提交的 `.agents/skills/`）里不归你管的个别 skill。
- **`agents_md_max_bytes` 配置与 AGENTS.md 截断提示**：AGENTS.md 总量预算从写死 32KB 变为 config.toml 可调（UTF-8 字节计），`0` 或负数 = 禁用 AGENTS.md 加载。预算不足发生截断或整篇丢弃时，启动后在转录区一次性提示受影响的文件路径、原始大小与实际注入量，不再静默截断。
- **子 agent 会话身份：持久化、按 id 续跑与留存策略**：每个子 agent 落盘为独立子会话（`<工作目录键>/subagents/` 下快照 + 全量日志 + 运行期活跃锁），不进主会话列表、不污染 `/resume` 与 `--continue`。`step subagents list / show <id> / delete <id>` 无头管理——show 完整回看子 agent 历史，解决子 agent 出错时父侧只收一句摘要、过程全丢的调试痛点；`/resume` 选择器新增「子 agent 会话」区，只读下钻完整历史。`spawn_agent` 加 `resume` 参数按 id 续跑：新指令追加为一条 user 消息、不替换已有历史，上次执行断在工具调用中途时自动补一条中断结果再续，持活跃锁的目标明确拒绝，续跑不占单会话派生配额；工具返回串带回子会话 id 供模型后续引用。新增 `[subagent.retention]` 配置：`delete_with_parent`（默认开，删主会话级联删子会话）、`max_sessions` 与 `ttl_days`（默认全 0 不主动删），清理只在进程启动时执行一次且一律跳过持锁会话；skill 激活计数随子会话快照持久化，resume 带回，递归防护不被续跑重置。
- **GitHub Release 分发（SEA 可执行文件 + npm tarball）**：v0.1.0 起每个版本发布两个产物——SEA 可执行文件（单文件、无需 Node 环境）与 npm tarball（`.tgz`、内含预编译 `dist/`）。用户可从 Releases 直接下载运行，或 `npm install -g <tarball-url>` 安装；源码安装与 `pnpm link --global` 路径继续保留。`package.json` 新增 `prepack` 钩子，确保 `npm pack` 在干净环境自动 build `dist/`，tarball 装完即用。

### Changed

- **Ctrl+C 忙碌时输入框优先**（行为变更）：忙碌中按 Ctrl+C，输入框有内容时先清空输入框（不中断回合），输入框为空才中断当前回合——与状态栏既有提示「Ctrl+C 清空输入框」一致，不再有输入内容被中断连带丢弃的情况。
- **OpenAI 协议渠道自报 User-Agent**：`openai` / `openai_responses` 协议的请求携带 `User-Agent: step-code/<version>`，不伪造也不隐藏客户端身份。
- **`--yolo` 与 `--auto` 互斥报错**（行为变更）：同时给两个权限 flag 属于笔误，此前 yolo 静默生效会掩盖真实意图，现在直接报错退出（exit 1）。

- **压缩时保留用户原话，不再只留一段摘要**（行为变更）：`fullCompact` 过去把被压缩段（含用户的原始提问）整体交给模型总结，压缩后用户当初说过的话只剩模型的二手转述，措辞一旦漂移，后续回合就会按错误理解继续干活（实测：压缩后把用户给过的项目路径当成没给过，转而凭推断乱猜）。现在压缩产物结构为「用户原话（保真）+ 交接摘要 + 最近消息」：被压缩段里真实的用户输入在 token 预算内（默认 20000，`[compaction] user_message_max_tokens` 可调，0 = 关闭退回原行为）以**独立消息**形态逐条**原样**保留，排在摘要之前；摘要垫在最后，让模型读完原话后最后看到的是「怎么继续」。保真消息在 storage 层有独立来源标记，因此**能跨多轮压缩存活**——每轮压缩都重新参与预算竞争，越旧的原话越可能在预算不足时被挤出，衰减是渐进的而非一到第二轮就全丢；它不计入会话轮次、不会被回退编辑当成「上一条用户输入」取回，但压缩过的会话会用它派生标题。预算不足时的取舍：最早的消息留开头（任务定义与全局约束在此）、最近的消息留结尾（当前意图在此，`user_message_head_tokens` 控制头部份额），单条超预算按方向截断并标注「本条前半/后半已截断」，其中最近段边界那条被截掉的前缀会回收到最早段（一条大 paste 因此头尾都能保住、只丢中间）；中段整条丢弃时插一条 system-reminder 写明省略了多少 token、内容由摘要覆盖，避免模型误以为用户从未说过（该提示每轮重建，不层层累积）。只收真实用户输入：工具结果回灌、系统注入的 reminder、模型自己的话都不进保真；纯确认语（「继续」「好的」「ok」这类整条只有确认语义的消息）也不占预算——它们信息量为零却会稀释注意力，判定只做整条全等匹配、不用长度阈值，因为「用方案 B」这类同样很短却载有决策的消息必须保留。保真消息占被压缩段超过 60% 时自动退回纯摘要形态（说明被压缩段本身太小，再搬一遍等于原地搬运而非压缩）。
- **压缩摘要 prompt 改为第一人称交接笔记**（行为变更）：原本六行功能性要求（进度、约束、待办、关键数据）扩写为一份带认知约束的 handoff 指令，每条都在防一类具体的压缩后事故——把「已经定下的决策」与「仍然待定的问题」分开写（防下一轮悄悄重开已关闭的选择，或把未定的当成已定）、保留执行过的确切命令与返回的具体值而非只留命令（防重跑代价高或不可重跑）、点名「下一步依赖但从未确认过」的缺口（防未知被当成已知）、要求把剩余步骤序列与已能预见的障碍一并写下而不止步于下一步（防计划退化）、对「测试通过了」「已修好」这类未验证声明明确标注为未验证（防假事实被继承）。system prompt 同步定调为「为未来的自己写笔记」而非第三方汇报。
- **micro 压缩加净收益门槛**（行为变更）：清理旧 `tool_result` 正文时，只清正文估算 ≥ 100 token 的块。更小的结果清掉几乎省不下 token（还要塞一条占位文本），净收益接近零，却白白丢掉可能有用的短结果（exit code、单行路径）并击穿 prompt cache 前缀。溢出保命路径仍把门槛降到 1 token（此时腾空间优先）。
- **溢出压缩重试改为递进收缩**（行为变更）：上下文溢出后的 3 次强制压缩重试原先每次跑同一套参数——第一次压不下去，后两次必然也压不下去，等于白烧三次摘要调用。现在按 `[0.7, 0.5, 0.35]` 逐次收紧，同时收缩「保留的最近消息条数」与「用户原话保真预算」，每次重试都比上次更狠。

- **上下文 token 估算算法改为字符分桶**：无法拿到服务端真实 usage 时（压缩后立即回落、无 usage 兜底、压缩时机判定的尾部补充、反思分段预算）使用的本地估算，从「全部字符数 ÷ 3」一刀切改为按字符分桶——ASCII 字符约 4 个算 1 token、非 ASCII（中日韩文字、全角符号、emoji 等）每字符算 1 token，更贴近真实分词。原口径对中文偏低、对英文偏高，中文内容的压缩时机与压缩后显示因此更准。图片仍按固定常数估算，不受影响。

- **忙碌态状态行独立成块（spinner 不再与文字挤在输入框内）**：思考/工具执行期间的转圈 spinner 从「挤在输入框行首、换帧时右侧文字视觉抖动」改为独立块置于输入框正上方——第一行是 `⠋ 状态词… (12s · ↓ 585 tokens)`（定宽 spinner glyph + 整轮固定的随机状态词 + 已用时间 + 本轮产出 token 估算），第二行是 Tip 提示。spinner glyph 用固定宽度容器包住，帧、计时、token 在同一次重渲里派生，换帧不再推移右侧文字。
- **子 agent 面板与任务清单的收尾显隐**（行为变更）：子 agent 全部进入终态时，完成摘要（头部计数 + 逐条类型/描述/工具数/状态）立即冻结进终端历史留痕，动态进度面板随即撤下，不再以完成态驻留动态区直到回合结束；下一波子 agent 启动时重建面板，每波各留一条摘要，滚动历史可回看到每个子 agent 的结果。任务清单全部完成时，回合收尾即清空面板（有未完成项则跨回合保留），不再全完成后仍永久常驻。
- **Windows shell 探测与回退策略强化**（行为变更）：`bash` 工具的 Windows shell 选择从「3 个固定路径找 Git Bash，找不到就回退 cmd.exe」升级为多级探测链——依次尝试 `STEP_SHELL_PATH` 环境变量覆盖、PATH 中的 bash、从 `git.exe` 与 `git --exec-path` 推断、Windows 注册表、固定候选路径来定位 Git Bash；Git Bash 之外再依次尝试 WSL、busybox-w32、PowerShell。**不再回退 cmd.exe**：cmd.exe 不识别 Unix 语法，与「用 Unix 语法写命令」的提示口径冲突，落到它会让命令静默失败、难以排查；四类 shell 皆不可用时直接给出可操作的报错（引导安装 Git for Windows 或设置 `STEP_SHELL_PATH`），把「无可用 shell」明确暴露而非伪装成可用。system prompt 的 shell 语法段改为按实际生效的 shell 动态生成——回退到 WSL / busybox / PowerShell 时提示对应语法约定，不再一律声称「通过 Git Bash 运行」。WSL 回退下自动处理工作目录（`/mnt/c/` 挂载转换）与 `NUL`→`/dev/null` 重定向改写。
- **skill 加载优先级修正**（行为变更）：同名覆盖顺序由「项目 < 用户」改为「用户级 < 项目 `.agents/skills/` < 项目 `.step-code/skills/` < `extra_skill_dirs` < 插件」——项目级现在覆盖用户级，原生目录覆盖兼容目录，符合就近覆盖的通行约定。

### Fixed

- **Markdown 表格在终端里完全不显示**：assistant 回复中的表格（`| 列 | 列 |` 形式）整块消失，只留下表格前后的文字，中间是一段空白——从画面上看像是模型没写完，实际内容一直都在。渲染层遗漏了表格这一类内容的处理分支，遇到表格既不报错也不留痕，直接跳过。现在表格正常渲染：表头加粗、下方一行分隔线、列与列之间用 `│` 分隔，列宽按实际显示宽度对齐（中文、日文等宽字符按两列计算，不会像按字符数对齐那样错位半格）。表格宽度超出终端时暂由终端自行折行，窄终端下的自适应排版后续再做。
- **网址在终端里被打印两遍**：消息中出现纯网址时（无论是直接写的裸链接、`<...>` 包裹的链接，还是链接文字与地址相同的写法），终端会显示成 `https://example.com(https://example.com)`，同一个地址重复两次，折行时还会在行尾留下 `(D` 这类断裂残片，长网址尤其明显。原因是渲染时对所有链接都在文字后面附上一份地址，而纯网址的文字本身就是地址。现在区分两种情况：`[文字](网址)` 这类有描述文字的链接仍显示为「文字 (网址)」，文字与地址是两份不同的信息；纯网址只显示一次。
- **忙碌中排队等待发送的消息在压缩/续接后滞留不发**：回合收尾时，自动续行（goal 驱动、hook 阻断续行）的判定排在发送队列派发之前且判定后不再回头消费队列，压缩等让回合变长的场景下队列会无限滞留；手动 `/compact` 的收尾也不排空队列。现在回合收尾统一走「队列优先于续接」的决策（两者同存时分两轮派发：本轮先发队首，续接留到队列消息回合的收尾），`/compact` 复用同一排空逻辑；审批/提问弹层存在时消息不提前出队，不再被守卫静默吞掉。
- **状态栏 context 用量在 `/new` 后不归零**：`/new` 开新会话清空了历史但未重置用量基准，状态栏继续显示上一会话的占用，直到下一次真实 usage 才校正。
- **`/reload` 不刷新当前模型的能力**：配置里给别名新增能力（如 image_in）后要等下次 `/model` 切换才生效；现在无论 provider 是否重建，`/reload` 都会刷新当前模型的 capabilities。

- **压缩摘要失控时会静默吞掉整段历史**：上下文压缩的摘要产出此前只检查「是不是空字符串」，非空即采用。实测事故：约 89 万 token 的会话被压成一条 **56 字符**的「摘要」，内容是模型复述历史片段而非交接笔记（形如 `[早期对话摘要]\n[调用工具 bash] 搜索项目中的配置文件……`），整段历史随即被它替换，全程无告警、不可恢复。现在摘要必须通过三道检查才会被采用：① 非空白；② 信息量下限——摘要 token 数不得低于「被压缩内容的 2%」，并封顶在 200 token（下限随压缩量上浮，压得越多要求越高；被压缩内容本身很小时几乎不设限，且下限永不超过原文体量，不会出现「要求摘要比原文长」的死锁）；③ 不含工具调用与媒体块的渲染标记（`[调用工具 X]`、`[工具结果]`、`[image ...]` 等），命中即视为模型在抄历史而非写笔记。任一项不合格就丢掉最老的一条消息、缩小输入后重新生成，最多尝试 3 次；三次都不合格则**放弃本次压缩、完整保留历史**（宁可不压，也不拿失效摘要替换历史）。同时补齐两处此前会直接放弃压缩的场景：摘要返回空内容、摘要请求遇到网络或 API 错误，现在都走同一条「缩小输入后重试」路径。

- **状态栏 context 用量在两次请求之间不更新（贴一大段文本后数字纹丝不动）**：context 占用原先只在每次 API 往返收尾时按服务端返回的真实 usage 刷新一次，因此刚贴进去、还没触发模型调用的内容完全不体现——看起来像"没算进去"。现在显示口径改为「已测量前缀 + 未测量尾部」：真实 usage 覆盖的部分用真实值，此后新追加、尚未经历一次 API 往返的消息用字符估算补上，提交内容后状态栏立即反映增量；下一条真实 usage 到达时校正该前缀（覆盖而非叠加），压缩后立即回落的行为不变。恢复会话时也不再显示 0，而是先按恢复的历史给出估算，直到首次真实 usage 校正。统计口径本身未变（仍是服务端 usage 的 `input + output + cache_read + cache_creation` 四字段全加）。

- **权限模式与模型不随会话持久化（恢复会话后退回默认）**：会话中切到 yolo/auto 或切了模型，关掉重开恢复会话后又退回启动默认。根因是权限模式压根没进会话存储（`SessionData` 无 mode 字段），模型虽存了却在恢复时被 `config.model` 无条件覆盖、且切换时不写回会话。现在权限模式与模型都作为会话级状态随会话落盘：`SessionData` 新增 `mode` 字段，`/permission`·`/yolo`·`/auto` 切换与 `/model` 切换即时落盘；恢复会话（TUI 内 `/resume` 与命令行 `-r`/`-c` 启动）读回会话存储的模式与模型（模型按存储值重建 provider），命令行 `--yolo`/`--auto`/`--model` 显式指定时仍优先覆盖；旧快照缺失字段时回退启动默认，向后兼容。
- **ask-user 的 Other 自由输入无法退出、输入时文本漂移**：进入 Other 编辑态后整个提问框的按键处理被 `ink-text-input` 独占——↑/↓/Esc 全部失效，想放弃自定义改选预设项时退不出来；且逐字符输入时文本视觉左移。改为编辑态不失活组件自身的按键处理，按状态分流：↑/↓（多题时 ←/→）退出编辑态回到选项导航且保留已输入草稿，可打印字符/退格/Home/End/删词等走与主输入框同一套自研文本编辑（弃用 `ink-text-input`，光标反色渲染稳定不漂移），Enter 提交、Esc 取消整个提问框。Other 草稿按题保存，退出后再进入原样恢复。
- **输入框与历史区用户消息排版错位**：长输入折行时「› 」前缀的空格被吞、折行续段只剩 1 空格缩进，与多行内容的 2 格悬挂缩进不一致。根因是 Ink 会把相邻的两个 Text 节点合并成一个文本块统一折行，前缀尾空格落在断行点时被折行算法吃掉。现在用户消息条目与输入框的正文都包一层独立容器，前缀与正文分开折行：空格稳定保留，续行统一保持 2 列悬挂缩进。
- **压缩后状态栏 context 用量不刷新（要等下一条消息才更新）**：上下文压缩（循环内自动压缩与手动 `/compact`）发生后，状态栏的 context 占用一直停在压缩前的旧值，直到下一次 API 响应带来真实 usage 才更新，看起来像压缩没生效。现在压缩完成后立即用压缩后历史的字符估算刷新状态栏（循环内自动压缩在 notice 之后补发一条 usage 事件，`/compact` 直接同步），下一条真实 usage 到达时再自动校正。
- **粘贴多行文本时换行错位（换行后内容显示到上一行行首）**：Windows 剪贴板的换行是 `\r\n`，粘贴后 `\r` 原样进输入值，终端按回车语义把后续内容写到行首覆盖，且 `\r` 会随消息发给模型。现在粘贴入口统一把 `\r\n` / 裸 `\r` 归一为 `\n`，多行内容按行正确显示；输入框行高预算同步按多行逐段折行计算（不再低估），光标停在换行符上时渲染不吞行。
- **busy 时发送消息入队，回执提示把流式回复劈成两截**：busy 中发送的消息入队后，「已加入发送队列」提示插在仍在增长的回复中间，后续正文另起一条显示（且前半截会提前冻结进历史）。现在 note 区分边界语义：UI 侧提示（队列回执、busy 中即时执行的斜杠命令输出等）透明，流式正文越过它续接、回复保持一整块；retry/notice/中止等 agent 流事件标记为边界，后续正文仍正确另起新条目。
- **图片占位符改造引入的启动崩溃（`Cannot access 'imageStore' before initialization`）**：`imageCount` 的 `useMemo` 声明位置在 `imageStore` ref 之前，`const` 的暂时性死区导致组件挂载即抛错、TUI 无法启动。将 `useMemo` 移到 `imageStore` 定义之后修复。
- **↑ 回溯到斜杠命令后无法继续向上翻历史**：历史回溯带出以 `/` 开头的命令文本时会触发补全菜单，↑ 随即被菜单选择劫走，用户被卡在菜单里。现在补全菜单只由「实际输入」触发——历史回溯（程序化回填）不弹菜单，可连续向上翻；随后任何改字（敲字符、删除）即重新武装菜单；提交/清空时抑制态复位。
- **正文流完后「已隐藏 N 行」挂到整轮结束才释放**：定稿判定从「最后一条 assistant 一律留动态区」修正为「仅末尾 assistant 留动态区」——text 事件只往末尾 assistant 追加，其后一旦出现任何条目（工具、提示、思考定稿），旧条目物理上不可能再增长，即时定稿进历史。长正文流完进入工具执行期后，全文立即落入 scrollback，不再被窗口化压整轮。
- **`web_search` / `web_image_search` 的 HTTP 451 误报为 key/额度问题**：搜索接口错误按状态码分流并透传服务端 message——451（平台内容安全审核拦截）明确提示调整检索词、与 key/额度无关；401/403 指向 key 配置、429 指向限流/额度、5xx 提示稍后重试。之前所有非 2xx 统一报「请检查 API key 与额度」且丢弃响应体。
- **`edit_file` 在 CRLF 文件上报「未找到 old_string」**：模型生成的 `old_string` 通常是 LF 换行，而 Windows 文件多为 CRLF，逐字符精确匹配会因 `\r` 失配。现在先按原样精确匹配，失败时归一化换行符（三方统一为 LF）重试匹配，写回时按文件原有换行风格恢复——CRLF 文件保持 CRLF、纯 LF 文件保持 LF，且不产生 `\r\r\n` 双重换行。唯一性检查同步在归一化后的文本上计算。
- **空流/空响应导致回合直接失败（`stream ended without producing a Message...`）**：服务端在产出任何内容前结束响应（网关/服务端瞬时故障）时，现在自动按指数退避重试，沿用「未输出内容才重试」的安全守卫；判定设在消费端，Anthropic 与 OpenAI 双协议通道同构覆盖。重试仍失败时给出明确的中文提示与处理建议，不再原样显示 SDK 英文报错。
- **工具执行期间滚动终端历史被瞬间拽回**：动态帧高度预算补完——新增 `liveBudget` 预算层保证动态帧总高恒 ≤ 终端行数 − 1；斜杠菜单与长输入折行精确计入预算；各面板长行截断；可选面板超限时按优先级降级；`LiveViewport` 测量时序竞态修复。新增 `STEP_DEBUG_RENDER=1` 渲染诊断日志（`%TEMP%/step-code-render-debug.log`）。
- **流式期间 TUI 整进程闪退（Maximum update depth exceeded）**：`LiveViewport` 在 commit 阶段的测量 dispatch 与流式 token 更新可形成自持级联，触顶 React 嵌套更新上限。新增同拍级联保险：连续同步测量 dispatch 超限即退到下一拍更新，级联被打断，正常路径行为不变。
- **API 错误信息不可读（如 `⚠ {"type":"error"}`）**：HTTP 错误包装层把可读摘要归一化到错误对象顶层 `message`（SDK 会忽略传入的 message 参数），展示层新增统一摘要：剥状态码前缀、提取服务端 type/message，输出 `HTTP 400 · invalid_request_error: <服务端消息>` 格式。
- **工具输出展开态被冻结进历史**：定稿条目恒以折叠态进入终端历史，Ctrl+O 展开仅为生成期间的临时视图，历史始终保持紧凑。
- **ask-user 提问 / 审批弹层出现时向上滚动被拽回顶部**：弹层的行数估算把每条逻辑行当 1 行，长题干、长选项描述、长命令预览在终端内折行后实际行数被低估，动态帧超高触发终端全清。改为按终端列宽逐行精确计算折行（宽字符按 2 列），多题时取最高一题。计划确认框、模型选择器、思考深度选择器的行数估算同轮一并校准。
- **思考模型「思考吃满输出预算」被误报为瞬时空响应**：思考模型（配 thinking 字段）在高思考档位下，思考消耗的 token 与正文共用 `max_tokens` 额度，档位预算逼近上限时正文没有空间生成，服务端返回仅含思考、无正文的响应。此前这类响应被统一判为「空响应」并当作网关瞬时故障反复重试——而重试毫无意义（预算组合不变必然复现），最终报出误导性的「请重新发送」。现在三层修复：① 默认 `max_tokens` 从 32768 提到 65536，给最高思考档位留足正文余量，开箱不再踩坑；② 空响应按 `stop_reason` 分型——`max_tokens` 截断且响应无正文/工具调用判为「思考耗尽预算」，不重试、直接给「调大 max_tokens 或用 `/think` 降低思考档位」的确定性提示，真正的瞬时空响应仍照原逻辑重试；③ 运行时 `/think` 切到当前 `max_tokens` 下正文余量不足的档位时，即时给出预算警告（不硬拦），补上运行期切档这条原先绕过配置校验的路径。
- **忙碌态状态行的 token 估算漏算思考与工具调用**：spinner 忙碌行 `↓ N tokens` 原先只累计流式正文，遗漏了思考内容（thinking）与工具调用的入参，且字符换算系数对中文严重低估，导致这行显示的产出量偏小、失真。现在思考内容与工具调用入参都计入本轮产出估算，换算系数与内部上下文估算（`estimateTokens`）统一。此改动仅影响忙碌行这处粗略估算；右下角 context 用量走服务端真实 usage（本就包含工具调用 token），一直准确、不受此问题影响。
- **`read_media` 未入只读白名单**：纯读一张本地图片在 manual / auto 档白弹一次确认框；plan 模式下被硬拦，模型收到「只能做只读调查、不能修改文件」的矛盾拒绝——读图恰恰是 plan 阶段的典型调查动作；非交互模式回灌「加 --yolo 或 --auto 放行」的误导引导（`--auto` 对它实际无效）。现已归入只读工具集，一处改动同时修好权限判定、plan 模式放行与非交互回灌三条链路。
- **`dynamic_workflow` 的 `agent(..., {phase})` 阶段标记凭空丢失**：`phase()` 原语会进工具输出日志，但 `agent({phase})` 形式只发展示事件、不进日志，任何地方都看不到。现统一经 emitPhase 记入日志缓冲并共享去重，phase 行随工具输出进返回给模型的报告。TUI 实时分阶段展示仍未接通，留待后续实现。

### Docs

- **三文件文档维护规范登记为 skill**：`AGENTS.md`（对内开发契约）、`CONTRIBUTING.md`（对外贡献指南）、`CHANGELOG.md`（对外用户-facing 变更记录）三文件的更新时机与边界正式纳入 `step-code-docs-maintenance` skill 管辖。核心规则：`docs/` 只放使用文档，设计稿一律进独立产品设计仓库；对外 CHANGELOG 只写用户能感知到的变化，不含内部架构术语；AGENTS.md 是开发契约不是设计文档；README 与 `docs/` 为双语，中文为事实源，改动其中一份必须同步另一份。
- **`step-code-install` 安装技能**：新增 `skills/step-code-install/`，面向「尚未安装 step-code 的用户」——把仓库 clone 下来后，让手上已有的 AI agent 读这份技能，即可代为完成环境检查、源码构建、配置 API key、验证与故障排查。内容只覆盖安装闭环，不含功能说明。该目录刻意不在各家 CLI 的自动扫描路径内，避免与工具目录冲突，技能顶部说明了两种取用方式。

### Changed

- **会话恢复改为事件重放**：恢复会话不再只依赖快照覆盖写，而是从最近检查点出发，重放后续事件流——崩溃后恢复的历史更完整，恢复过程本身不产生新事件、不触发额外网络请求；旧会话仍可正常打开。
- **后台任务通知改为结构化消息**：后台任务完成通知从「模拟用户输入」升级为带来源标记的结构化消息，resume 时能区分通知与真人输入，不会把历史通知当成用户输入重复计入轮次；幂等投递避免同一任务重复提醒。
- **provider 请求边界层重构**：消息整形、能力表查询、主动降级、错误重投影从 provider 内部收敛到显式边界层，新服务商接入只需新增 adapter，不影响核心对话循环。

### Fixed

- **子 agent 会话锁 stale 修复**：子 agent 进程被强杀后，残留锁不再卡死整个子 agent 目录——现在可按 id 删除断影子 agent 会话，残留锁在 resume 与删除时自动清理。
