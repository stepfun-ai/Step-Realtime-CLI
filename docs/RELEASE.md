# Release（占位）

待补：

- 版本号策略（SemVer / 0.x 阶段）
- changelog 生成（建议 `changesets` 或手写）
- npm publish 流程（当前 `package.json` 是 `"private": true`，未走 publish）
- GitHub Release notes 模板
- 客户端二进制（`bin/`）的发布通道

owner 完善前，本文件仅作为占位，不应阻塞 PR。
