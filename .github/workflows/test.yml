name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Parse Windows PowerShell installer
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $tokens = $null
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile(
            "scripts/setup.ps1",
            [ref] $tokens,
            [ref] $errors
          ) | Out-Null
          if ($errors.Count -gt 0) {
            $errors | Format-List | Out-String | Write-Error
            exit 1
          }

      - run: pnpm lint
      - run: pnpm exec tsc --noEmit
      - run: pnpm test

      - name: Build on Windows
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: pnpm build

      # Sentinel for #25-class regressions: warns (does not fail) when the
      # OpenTUI runtime chunk is missing its exports. Runs only on the Windows
      # lane (which already produces a build); tsdown output is platform-shape-
      # consistent, so one lane covers the build regression. Non-blocking on
      # purpose so it can land independently of #28; the warning clears once
      # #28 lands. Flip to `--enforce` (and drop continue-on-error) for a hard gate.
      - name: Build-output sentinel (#25 regression guard)
        if: matrix.os == 'windows-latest'
        continue-on-error: true
        run: node scripts/check-build-output.mjs

      - name: Install with Windows script
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: ./scripts/setup.ps1 -SkipInstall -SkipBuild

      - name: Smoke installed Windows launcher
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $env:Path = "$HOME\.step-cli\bin;$env:Path"
          step --version
          step aec status
          step vad status
          step voice --help
